import React from "react";

const Loading = (state) => {
  //   console.log(state);
  return (
    <>
      <div className="loader">
        <h1>Loading...</h1>
      </div>
    </>
  );
};

export default Loading;
